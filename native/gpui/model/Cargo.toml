[package]
name = "carnet-model"
description = "Le format de document Carnet, lu et écrit par une troisième implémentation — sans dépendance d'interface, comme NbeModel côté Swift."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
loro = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
