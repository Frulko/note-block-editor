node_modules/
dist/
.DS_Store
*.log
site/dist
site/.astro

site/.data
test-results
playwright-report

# Native build output. Both of these were committed by accident: `cargo check`
# and `swift test` create them, and `git add -A` does not know better. The
# history was rewritten to remove 764 MB of it.
apps/desktop/src-tauri/target/
native/swift/.build/

# The iOS project is generated from `project.yml` by xcodegen, and its build
# directory holds a 40 MB WebRTC xcframework. The yml is the source of truth;
# the pbxproj is output, and a committed one is a merge conflict waiting.
apps/ios/Carnet.xcodeproj/
apps/ios/build/
