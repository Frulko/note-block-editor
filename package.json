{
  "name": "notion-block-editor",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter demo-vanilla dev",
    "build": "pnpm --filter demo-vanilla build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
